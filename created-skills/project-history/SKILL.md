---
name: project-history
description: Use when you need historical context on how a feature was built, why a past decision was made, or the validation/test-count trail for a completed milestone in this project — the changelog of every marco/fase from Marco 1 through the most recent branch, moved out of CLAUDE.md so it isn't always loaded.
---

# Histórico de Marcos — SocialMedia

Changelog cronológico de tudo que foi concluído no projeto, com contexto de decisão e validação. Consulte quando precisar saber **por que** algo foi feito de um certo jeito, **quando** foi feito, ou **quantos testes** passavam num ponto específico do histórico.

- **Marco 1 (Fundação + Multi-tenancy): Concluído** — scaffold, db:bootstrap (cria socialmedia e socialmedia_test), Sanctum SPA sessão, tenancy Agency/Client/SocialMediaCredential com AgencyScope, registro auto-agência, API clients/credentials, toolchain Vite/UnoCSS/Ziggy com libs Max* em storage/libs, SPA com login/registro (MaxAuthCard), dashboard de clientes e workspace com credenciais. Suíte Pest 17/17.
- **Marco 2 (Core Social Media): Concluído** — 8 tabelas (brand_positionings, social_media_characters, social_media_themes, calendar_events, calendar_event_script_details, calendar_event_script_detail_character, calendar_event_artworks, catálogos platforms/type_posts), brand positioning (upsert + assets + campo `activities`), characters (CRUD + imagens), themes (CRUD + conteúdos + aprovação → cria evento no calendário), calendário editorial com máquina de estados manual `CalendarEventStatus` (planned→...→published) e artworks com aprovação/rejeição, catálogos platforms/type-posts, workspace do cliente com tabs (Calendário FullCalendar, Marca, Personagens, Temas) e modal de evento com workflow completo. Suíte Pest 52/52. E2E via curl confirmou fluxo completo (registro→client→brand→character→theme→approve→evento planned→...→published) e isolamento entre agências (calendar vazio, 404 em brand/theme/character de outra agência).
- **Marco 3 (Agentes de IA): Concluído** — infra (`laravel/ai` + Gemini, Horizon com fila dedicada `gemini`, Redis, campo `ai_enabled` (depois renomeado para `client_enabled`)/`ai_settings` em `clients`, tabela polimórfica `agents_ai_cost`/`AgentAiCost` via trait `HasAiCost`, tabela `calendar_event_arts_analysis`); 7 agentes (+ `NewsRelevanceAgent`, adicionado depois — ver regra 14) (`App\Ai\Agents`: Copywriter, CopywriterReviewer, GraphicEditor, ArtAnalyst, StrategyManager, ThemeExtractor, EventReplanner) com tools escopadas por client (`App\Ai\Tools`) e trait `HasAgentAiRequest` (loop do-while, fallback de modelo, custo/benchmark no canal de log `gemini`); 7 Jobs (`App\Jobs\Ai`) na fila `gemini`, encadeados automaticamente pelo mapa único `App\Services\SocialMedia\AiPipeline` (usado tanto pelo `CalendarEventObserver` quanto pela trait `AdvancesEventStatus` das escritas guardadas dos próprios Jobs) — tudo **gated por `client->ai_enabled`** (hoje `client_enabled`, ver regra 11; default `false`; desligado, o fluxo M2 permanece 100% manual, sem nenhum Job enfileirado); novos estados `art_analysing`/`replanning`; UI do workspace com toggle de IA, geração/replanejamento de plano, badges de status e custos por evento/cliente; comando `php artisan ai:smoke {clientId}` para validação manual com chamada real ao Gemini (fora da suíte). Suíte Pest 135/135. Regressão via curl confirmou que, com o flag em `false`, `approve-planning` não enfileira nada na fila `gemini` (Redis). `ai:smoke` real validado (script gerado, evento `planning_approved→script_drafted`, custo persistido). **Nota de unidade:** `AgentAiCost.total_duration` é em **milissegundos inteiros** (fonte: campo `total_duration_ms` de `HasAgentAiRequest::execute()`; `saveAiCost` normaliza com `(int) round()` como salvaguarda — o float de segundos que os Jobs passavam antes fazia o Postgres rejeitar o INSERT e o custo era descartado silenciosamente; bug pego pelo `ai:smoke` real, corrigido com teste de regressão em `AiInfraTest`).
- **Marco 4 (Integrações Meta/News/BRL): Concluído** — `Webhook` (payload bruto Meta) + `MetaWebhookController` público (verify/receive, assinatura HMAC opcional via `META_APP_SECRET`) + `MetaWebhookJob` (fila `webhooks`); publicação automática `scheduled→published` via `App\Services\SocialMedia\EventPublishingService` (idempotente por alvo) e `PublishEventJob` (`ShouldBeUnique`, fila `default`), disparado por comando agendado (`publish-due`) ou manualmente (`publish-now`) — falha em algum alvo grava `publish_error` e mantém `scheduled`; notícias RSS por cliente (`social_media_news`, `client->news_keywords`, `RefreshNewsJob`, agendado periodicamente) com aprovação → `SocialMediaTheme` draft (+ extração IA) e reprovação; `ExchangeRateService` (cotação USD→BRL cacheada, fallback `5.50`) usado por `AiCostController` (`total_usd`/`dolar_rate`/`total_brl`); UI de credenciais Meta, aba de notícias no workspace e chip de custo em BRL. Suíte Pest 192/192. E2E via curl (`php artisan serve` + cookie jar/XSRF, com `Referer` obrigatório para sessão stateful do Sanctum) confirmou: registro→client→`news/keywords`→`news` vazio→aprovação de notícia via tinker (201, tema draft criado e visível em `GET themes`)→evento `scheduled` sem credenciais→`publish-now` (200) + job síncrono deixando `publish_error` preenchido e status ainda `scheduled`→webhook Meta (`hub_verify_token` correto responde `hub_challenge`, incorreto → 403)→isolamento entre agências (`GET news` de outra agência → 404)→`GET ai-costs` retornando `dolar_rate`/`total_brl` reais.
- **Marco 5 (LoRA por Personagem): Concluído** — treino de LoRA por personagem via botão "Treinar personagem" (cooldown `LORA_COOLDOWN_HOURS`, mínimo `LORA_MIN_IMAGES` imagens), chain `App\Jobs\Lora\CaptionCharacterImagesJob` (fila `gemini`, captions via Gemini + trigger word `chr_{slug}_{4ulid}`) → `App\Jobs\Lora\TrainCharacterLoraJob` (fila `lora`, 1 processo dedicado, OneTrainer CLI para Z-Image, template `resources/lora/zimage-lora.template.json`) produzindo `.safetensors` copiado para `COMFYUI_LORA_DIR/socialmedia/{character_id}.safetensors` (ULID); progresso em `character->lora_status` (`none|pending|training|ready|failed`); agentes de IA (`GetBrandPositioning`) só enxergam personagens `ready`; roteamento do evento em `script_ready` passou a considerar **personagens vinculados ao roteiro** (`scriptDetails()->whereHas('characters')`, independente de `lora_status`) — com vínculo → `App\Jobs\Lora\GenerateCharacterArtJob` (ComfyUI via `App\Services\SocialMedia\ComfyUiService`, template `resources/lora/comfyui-zimage-character.json`; internamente usa apenas LoRAs de personagens `ready`), sem vínculo → segue `GraphicEditorJob` normal; falha na geração grava `generation_error` e mantém `script_ready`; endpoint manual `POST events/{event}/generate-art` (à época independente de `ai_enabled`; hoje exige `client_enabled=true`, ver regra 11/16); comando `php artisan lora:smoke {characterId} [--generate]` para validação manual com OneTrainer/ComfyUI reais (fora da suíte); corrigido bug pré-existente em `GenerateEventArtwork::characterAttachments` (esperava `images` como algo diferente de `string[]`); UI: badge de status + botão + modal de confirmação (48h) na aba Personagens, seção "Gerar arte com personagem" no modal do evento. OneTrainer e ComfyUI rodam na mesma máquina do Laravel (GPU local). Suíte Pest 241/241.
- **Validação real do Marco 5 (GPU local, 08/07/2026): OK** — ambiente configurado no `.env` (OneTrainer/ComfyUI via StabilityMatrix em `/opt/stabilitymatrix/Data/Packages/*`, LoRAs em `Data/Models/Lora`; geração com `z_image_bf16.safetensors`, treino com `Tongyi-MAI/Z-Image` — modelos distintos: `ZIMAGE_BASE_MODEL` vs `ZIMAGE_TRAIN_MODEL`); templates de `resources/lora/` derivados dos oficiais (workflow `image_z_image` do ComfyUI 0.27: CLIPLoader `qwen_3_4b`/`lumina2` + `ModelSamplingAuraFlow` shift 3 + KSampler `res_multistep`; preset `#z-image LoRA 16GB` do OneTrainer, com `concepts.json` separado por personagem, `prompt_source=sample`). **Gotcha do OneTrainer:** o config parcial deve ir TAMBÉM em `--preset-path` (migrate=false); só `--config-path` aciona o migrador de configs antigos, que quebra em configs parciais. `lora:smoke` ponta a ponta: captioning Gemini (custo em `agents_ai_cost`) → treino real (~30 min, `.safetensors` 67MB) → `--generate` produziu retrato fiel. **Nota operacional:** a aba Inference do StabilityMatrix compete pelo ComfyUI (enfileira prompts próprios e emite interrupts globais que matam gerações do app) — não usar durante treino/geração.
- **Kill switch `client_enabled` (renomeação + ampliação de `ai_enabled`, 09/07/2026): Concluído** — campo `ai_enabled` renomeado para `client_enabled` em toda a base (migration `renameColumn`, model, controllers, pipeline, frontend, testes); método `Client::isEnabled()`; semântica ampliada de "gate de IA" para kill switch geral do cliente (ver regra 11): `publish-due` e `fetch-news` filtram por `client_enabled=true`; guards 422 `'Cliente desabilitado.'` em `CalendarEventController::store`, `ThemeController::approve`, `NewsController::approve` e todas as transições de `EventWorkflowController` exceto `publishNow` (aberto p/ retry de `publish_error`); `generate-art` perdeu a exceção antiga e agora exige `client_enabled=true`; `rejectArtwork` sempre roteia p/ `art_analysing` (branch manual morto removido). Suíte Pest verde (só `ExampleTest` de Vite manifest falha, pré-existente/ambiental).
- **Aba Temas alinhada ao EngeApp (27/07/2026): Concluída** — ver regra 17. Backend: enum `ThemeStatus` reescrito, migration `2026_07_27_000002` (novo flag `is_ready_for_calendar`, conversão dos status antigos `draft/archived→elaborating` e `ready/approved→waiting+ready_for_calendar`, e dos `base_contents` posicionais para objetos com `id`), `ThemeController` no modelo do EngeApp (upload/add/remove de conteúdo por `id`, approve = extrair), `ThemeScheduleService` + `GetReadyThemes` com datas expandidas (fecha a lacuna do prompt do `StrategyManagerAgent`, que já citava `datas_pendentes`/`agendamento` sem a tool correspondente), extração real por tipo no `ThemeExtractionJob` (nova dependência `spatie/pdf-to-text`; exige `pdftotext` do poppler no sistema), rota `themes/{theme}/extract` removida (`AiActionController::extractTheme` saiu). Frontend: `ThemeCard`/`ThemeContentList`/`ThemeScheduleBuilder` portados, `TabThemes` com sub-abas Pendentes/Agendados/Publicados/Notícias/Arquivadas (as duas últimas montam `TabNews` com a prop `archived`, e a aba Notícias saiu do `ClientWorkspace`), store `useThemesStore` reescrita e feedback por `Toast`. O trabalho anterior de "data opcional" foi revertido (tools `GetUnscheduledEvents`/`ScheduleCalendarItem` e seção "Sem data" removidas); `calendar_events.start_at` ficou **nullable** (migration `2026_07_27_000001`) como salvaguarda transitória — **voltou a `NOT NULL`** no marco seguinte (ver abaixo). Suíte Pest 267/273 (só `ExampleTest` de Vite manifest falha, pré-existente/ambiental); `vue-tsc` e `vite build` limpos.
- **Planejamento mensal alinhado ao EngeApp (27/07/2026): Concluído** — modal com mini-calendário e estratégia de conflito por dia, modo "somente temas pendentes", mapa de datas (`date_strategies`) atravessando Job/Agente/Tool (`App\Ai\Tools\CreateCalendarItems`) com a regra de conflito aplicada no código, fim do evento sem data (`calendar_events.start_at` de volta a `NOT NULL`, migration `2026_07_29_000001`, apagando os registros órfãos remanescentes). As tools `GetUnscheduledEvents`/`ScheduleCalendarItem` já não existiam — foram removidas antes, junto com a aba Temas (ver entrada acima). Ver regra 18.
- **Modal do evento alinhado ao EngeApp (30/07/2026): Concluído** — ver regra 19. Backend: `art_ready` ganhou transição direta para `scheduled` na matriz; `approveArtwork`/`rejectArtwork` deixaram de transicionar o evento (viraram só o status da arte + `positive_points`/`negative_points`, rejeição agora exige `negative_points` com `min:10`); novo `POST events/{event}/continue` (`EventWorkflowController::continueEvent`) decide `scheduled` (tudo aprovado) ou `art_analysing` (alguma rejeição, gravando `general_observation` em `rejection_observations`) a partir de `art_ready`; novos `PUT events/{event}/script-details/{scriptDetail}` e `.../characters` (bloqueados em `scheduled`/`published`); `EventWorkflowController::loadedEvent()` centraliza os eager loads (`scriptDetails.characters`, `scriptDetails.artwork`, `artworks`, `artsAnalyses`, `sourceTheme`, `aiCosts`) usados por todo endpoint de workflow e por `CalendarEventController::show`; `updateScript` só reconcilia a lista de slides quando `details` está de fato na request (antes, ausência da chave apagava todos os slides). Frontend: `SocialMediaEventModal.vue` reescrito — cabeçalho com tag-selects de formato/mídia, autosave de título/briefing (debounce 600ms), stepper de 4 passos, abas Briefing/Script/Artes/Publicação com aba inicial por status, sub-abas por slide com lightbox/pontos positivos-negativos/aprovação por slide e caixa "Continuar", preview estilo Instagram + painel de publicação, rodapé com Reiniciar do Zero/Excluir/Aprovar Briefing/Voltar ao Briefing. Preservado do que só este projeto tem: geração de arte com personagem via LoRA, upload manual de arte, insights/comentários pós-publicação, polling de 10s. Bloco "Replanejar com IA" saiu do modal (endpoint continua existindo). Suíte Pest 347/347.
- **Lista de clientes com métricas de produção (27/07/2026): Concluída** — `/clients` deixou de mostrar `CPF/CNPJ` (o campo continua no cadastro) e passou a mostrar, por cliente, `Briefing` (todos os status anteriores a `scheduled`, via novo `CalendarEventStatus::briefing()`), `Agendadas` (`scheduled`), `Publicadas` (`published`), `Total de Postagens` e `Ativo` (`client_enabled`, somente leitura). Os contadores vêm em uma única query (`withCount` filtrado) no `GET /api/clients`. Coluna de ações com atalhos para o workspace já na aba certa (`/clients/{id}?tab=calendar|themes`, aba lida/escrita na query em `ClientWorkspace`) e exclusão do cliente com confirmação. **A exclusão é um soft delete em cascata recuperável** (ver regra 20): `Client` já usava `SoftDeletes`, então o cascade de FK do banco nunca dispara — quem propaga é o novo `App\Observers\ClientObserver`. Suíte Pest 359/359 após o merge em `master` (a exclusão exige `agency_admin`, e `/login`, `/register` e `/api/user` passaram a devolver `roles`).
- **Treino de LoRA em chunks com fila de GPU priorizada (2026-07-27): Concluído** — ver regra 16; spec em `docs/superpowers/specs/2026-07-27-lora-chunked-training-design.md`; suíte Pest verde. **Pendente de validação na GPU real**: rodar `php artisan lora:smoke {characterId}` uma vez antes do primeiro treino de produção para confirmar `continue_last_backup`/nomes de chaves do TrainConfig da versão instalada do OneTrainer.
- **Relevância de notícias por IA + contadores nas abas (2026-08-03): Concluído** — ver regra 14; spec em `docs/superpowers/specs/2026-08-03-news-relevance-design.md`, plano em `docs/superpowers/plans/2026-08-03-news-relevance.md`. Novo agente `NewsRelevanceAgent` (8º agente do projeto), tool `SaveNewsRelevance`, Job `NewsRelevanceJob`, colunas `relevance`/`relevance_reason` em `social_media_news`, endpoint `GET news/counts`, store `useNewsCountsStore` e chip de relevância no `TabNews`. Suíte Pest 382/382.
- **Detecção de notícias duplicadas por LLM local (2026-08-03): Concluída** — ver regra 14; spec em `docs/superpowers/specs/2026-08-03-news-dedup-design.md`, plano em `docs/superpowers/plans/2026-08-03-news-dedup.md`. Novo `NewsStatus::Duplicated`, `OllamaProcessManager` (ciclo de vida sob demanda + `unloadModel`, com `spawnUsing()`/`killUsing()` injetáveis para teste sem subir processo real), `NewsDedupService` (prompt validado experimentalmente, parsing tolerante a cercas ```json, validação de escopo dos ids), `NewsDedupJob` (fila `ollama`, supervisor próprio, conexão `redis-ollama` com `retry_after` **960s** > timeout 600s do Job), chip de duplicada no `TabNews`. Suíte Pest 415/415.
- **Supervisor único de GPU (2026-08-03): Concluído** — `lora-supervisor` renomeado para **`gpu-supervisor`** e passou a consumir também a fila `ollama` (`['ollama','lora','lora-train']`, `balance=false`, 1 processo). Motivo: `ollama-supervisor` e `lora-supervisor` eram independentes e podiam rodar ao mesmo tempo, somando o modelo do dedup (~9,6 GB) a um treino Z-Image e estourando a VRAM da GPU local — falha silenciosa e cara (OOM da CUDA, chunk de treino perdido). Um supervisor só serializa toda a GPU. A conexão `redis-ollama` foi removida (órfã): um supervisor admite uma conexão, e a `redis-lora` (`retry_after` 7500s) cobre o maior timeout do grupo (1800s). Regressão coberta por `GpuQueuePriorityTest` (inclui um teste que falha se qualquer outro supervisor voltar a consumir fila de GPU) e `LoraInfraTest`. Suíte Pest 421/421.

- **Qualidade do prompt de imagem (2026-08-04): Concluída** — spec em `docs/superpowers/specs/2026-08-04-image-prompt-quality-design.md`, plano em `docs/superpowers/plans/2026-08-04-image-prompt-quality.md`; ver regra 16. As artes com personagem saíam com os rótulos do briefing **desenhados dentro da imagem** (`CENA/SUJEITO`, `PALETA DE CORES`, `#F5A623`, `Character_ids:()`), texto deformado e ilegível, e reels em 1024×1024. Causa raiz: `GenerateCharacterArtJob::promptFor()` concatenava `visual_briefing . '. Texto do slide: ' . visual_text` e mandava cru ao `CLIPTextEncode` — o CLIP não distingue metadado de conteúdo. Somava-se a isso a ausência de tradução (as LoRAs dos personagens são treinadas com captions em **inglês** pelo `CaptionCharacterImagesJob`, então prompt em português é incoerente com o domínio da LoRA), o nó negativo com string vazia fixa e o `aspect_ratio` que o `ComfyUiService` suportava mas nenhum caller passava. **Não foi trocado o modelo base**: qualquer modelo desenha `PALETA DE CORES` se a string estiver no prompt, e trocar o checkpoint invalidaria todas as LoRAs já treinadas. Novo `PromptBuilderAgent` + `ImagePromptBuilder`/`ImagePrompt` (com fallback determinístico), `{{NEGATIVE_PROMPT}}` no template e `aspect_ratio` em reels. Suíte Pest 470/471 (só o `ExampleTest` de Vite manifest falha, ambiental/pré-existente). **Etapa 2 pendente (spec próprio):** overlay tipográfico do `visual_text` sobre a arte — hoje as artes saem **sem texto nenhum**, com espaço reservado no terço inferior. **Pendente de validação na GPU real:** rodar um `generate-art` e comparar com as artes-problema — a suíte prova que o prompt está limpo, não que a arte ficou boa. **Achados diferidos:** `HasAgentAiRequest` faz `sleep(3)` incondicional ao fim do do-while mesmo com `max_calls=1` (~3s por slide com a GPU reservada; afeta os 10 agentes, não só este); falta teste E2E `EventWorkflowController → Job → ComfyUiService` para `seed=0`; critério `format === 'reels'` duplicado entre `GenerateCharacterArtJob` e `GenerateEventArtwork` (comentário cruzado mitiga); `NewsDedupJob` roda pelo Gemini e **nunca chama `saveAiCost`** — o dedup de notícias não aparece no dashboard de custos (lacuna pré-existente, descoberta durante este trabalho).

- **Logomarca obrigatória na arte do primeiro slide (2026-08-06): Concluída** — spec em `docs/superpowers/specs/2026-08-06-brand-logo-in-artwork-design.md`, plano em `docs/superpowers/plans/2026-08-06-brand-logo-in-artwork.md`; ver regras 10 e 16. Nova coluna `brand_positionings.logo_path` (arquivo único, separada de `assets`) com endpoints e seção própria na aba Marca; `GenerateEventArtwork` anexa a logo em primeiro lugar e prefixa o prompt com instrução obrigatória no slide 1 / opcional nos demais; removido o `logo` da lista de negative prompt sugerida no agente e na tool. Fora do escopo: caminho LoRA/ComfyUI. **Pendente de validação na GPU real:** rodar um `generate-art` num cliente com logo e conferir se a logomarca sai fiel e discreta — a suíte prova que a instrução e o anexo chegam ao Gemini, não que a arte ficou boa.
- **Auditoria + correções em fases (Fases 1-6, 06-07/08/2026): Concluídas** — auditoria documentada em `implementations/` (README + PLANO com 33 achados), corrigida em 6 fases sucessivas, cada uma em worktree isolado (`.worktrees/wt-<slug>`) com TDD (teste que falha antes da correção) e merge em `dev`. **Fase 1 (segurança):** mime real no upload de tema, `renderMarkdown` com escape de HTML (`resources/Js/markdown.ts`, 1º teste vitest do projeto), `hash_equals` no token do webhook Meta, mensagem estável de erro da Graph API no `CommentController`. **Fase 2 (integridade de dados):** índices únicos parciais (`WHERE deleted_at IS NULL`) para conviver com soft delete de `Client` (regra 20), lookups com `withTrashed()`/`Rule::exists` corrigidos, hooks `restoring`/`restored` no `ClientObserver`. **Fase 3 (destravar produto):** `updateScript` apaga artes órfãs antes de apagar slides, `hasPendingSlides` do frontend alinhado ao backend, `ThemeExtractionJob`/`ArtAnalystJob::failed()` revertem o status em vez de travar o evento. **Fase 4 (dano irreversível):** `EventDeletionService` compartilhado, `CreateCalendarItems` recusa `replace_one` sobre evento `published`, `continueEvent` recusa `start_at` vencido, guarda de identidade em `useEventDetailStore`. **Fase 5 (resiliência de IA):** `HasAgentAiRequest::MODEL_FALLBACK` explícito, `saveAiCost` antes de `throwIfIncomplete` em 6 Jobs, `NewsDedupService::findDuplicates` passou a chamar `saveAiCost`, `uniqueFor` calculado (não implícito) em 9 Jobs `ShouldBeUnique`. **Fase 6 (kill switch/filas/GPU):** `news/refresh` e `characters/{id}/train` passaram a checar `client_enabled`; `OllamaProcessManager` removido do `NewsDedupJob` (o dedup roda 100% pelo Gemini desde a regra 14 — o Job só carregava um processo Ollama inútil, furando a prioridade da fila `ollama` na frente de treinos/gerações reais; fila mantida por nome por decisão deliberada, ver docblock do Job); `PublishEventJob::handle()` revalida `client_enabled` (exceto `manual: true`); timeout do supervisor `default` do Horizon elevado de 60s→320s (cobre os 300s do `PublishEventJob`, que já não podia terminar antes do Horizon matar o worker); `QueueRetryAfterTest` ganhou a 3ª dimensão (`supervisor.timeout` vs `Job.timeout`, além das duas já cobertas de `retry_after`); regra 12 atualizada para descrever o retry automático real (`tries=2`, backoff 60s/180s) além do manual via `publish-now`; `publishNow` limpa `publish_error` antes de enfileirar (evita a UI mostrar erro obsoleto enquanto a nova tentativa aguarda na fila); `ComfyUiProcessManager::forceStop()` (novo) chamado no `failed()` do `GenerateCharacterArtJob` — timeout do worker não passa pelo `finally` do `handle()`, e a instância que fez o spawn morre com o worker, então só o pidfile em disco (não `$owned` em memória) pode dizer com segurança qual processo é nosso para liberar a VRAM. Suíte Pest verde (só `ExampleTest` de Vite manifest falha, ambiental/pré-existente).
- **Fase 7 (UX, tipagem e documentação, 07/08/2026): Concluída** — última fase da auditoria em `implementations/`, worktree `wt-fase7-ux`. **Tipagem (7a):** `vue-tsc` estava quebrado por 3 causas distintas — `auto-import.d.ts`/`auto-import-components.d.ts` (gerados pelo Vite, nunca commitados e ausentes até a 1ª build) não existiam no worktree (resolvido rodando uma build antes de checar); `SimulatedSlide`/`SlideWithArtwork` divergiam do shape real do slide (`artwork.url` vs `artwork.status`), consolidados em `resources/Types/socialMedia.ts` (`EventSlide`); e `pinia`/`vue-router` das libs symlinkadas `storage/libs/MaxPinia`/`MaxUse` (repos externos, cada um com seu próprio `node_modules`) estão em versões minor divergentes das do projeto (pinia 4.0.2 vs 3.0.4, vue-router 5.2.0 vs 5.1.0) — dedup via `overrides` do `package.json` só alcança pacotes hoisted no `node_modules` do próprio projeto, não os das libs `file:` (tentativa revertida); os 2 erros resultantes em `resources/app.ts` foram isolados com `@ts-expect-error` documentado (raiz é externa a este repo). Script `typecheck` (alias de `type-check`) adicionado. **Modal do evento (7b, `SocialMediaEventModal.vue`):** `saveSlideDetail` (endpoint já existia, testado, sem UI) ligado via autosave debounced 600ms nos campos "Texto na arte"/"Briefing visual", condicionado a `canEditScript`; `activeTab` só é realinhado com `force=true` (poll de 10s deixou de arrancar o gestor da aba onde está); `updateEvent()` serializa escritas numa fila (`updateEventQueue`) com `requestId` monotônico que descarta reload() obsoleto — título/instructions/slide não competem mais por escritas concorrentes; `close()` virou `async`, aguardando os saves pendentes antes de fechar (erro de um PUT que falha no fechamento agora aparece, em vez de ser perdido num modal já fechado); os dois watchers de autosave (título/instructions) passaram a checar `canEditTitle` antes de agendar o PUT (parava de reescrever posts `published`). **Kill switch na UI (F-05):** banner "Cliente desabilitado" no topo do modal + `:disabled="!clientEnabled"` nas 9 ações que o backend recusa com 422 (Aprovar Briefing, Reiniciar do Zero, Voltar ao Briefing, Excluir, Aprovar/Reprovar arte, Continuar, Gerar arte com personagem, Upload manual) — `publishNow` deliberadamente NÃO desabilitado (única exceção documentada da regra 11); toggle do kill switch (`ClientWorkspace.vue`) trocado de `v-model` direto na store para `:model-value`/`@update:model-value` — o v-model mutava `client.client_enabled` na store ANTES do PUT resolver, então um PUT que falhasse deixava a store com o valor errado e o "revert" (remount via `enabledSwitchKey`) relia esse mesmo valor incorreto; `enabledToggleError` passou a ser renderizado. **Remoções destrutivas (F-07.7):** `deletePlatform`/`deleteTypePost`/`removeCredential` (todas `forceDelete`, hard delete irrecuperável pela regra 20) ganharam modal de confirmação + `:loading` (guarda de duplo clique) no mesmo padrão do `delete_dialog_ref` de `TabCharacters`; `removeCredential` também ganhou o `catch` que faltava. **Planejamento mensal (F-07.5):** trocar de mês parou de resetar as datas/estratégias já marcadas — o default (Seg/Qua/Sex) só é aplicado quando a seleção está vazia, e a inicialização saiu do `watch([planMonth, planYear])` para `openGeneratePlanModal()`. **Backend (7d):** `NewsController::approve` recusa `duplicated` citando a notícia vencedora (extraída do prefixo determinístico de `relevance_reason`) mas permite resgatar `reproved` (decisão deliberada — relevância pode mudar de contexto); `ThemeController::validateTheme` passou a validar a estrutura de `schedule` por `type` (`in:specific_dates,weekly,monthly_days,monthly_nth_weekday` + campos específicos de cada regra) — antes um `schedule` mal formado passava com 200 e só quebrava depois, dentro do `StrategyManagerJob` na fila, sem nenhum retorno na UI; `CalendarEventController::index` passou a validar `start`/`end` como `date` (era só `filled()`, e um valor ilegível virava 500 do Postgres em vez de 422); `EventPublishingServiceTest` teve o teste do fallback de caption (`$event->title` quando `script` é `null`) dividido em dois casos — o original nunca alcançava essa linha porque testava sem arte aprovada (o service retorna cedo antes de montar o `caption`). `.env.example` ganhou as 4 chaves documentadas em código mas ausentes do arquivo (`OLLAMA_PID_PATH`, `COMFYUI_PID_PATH`, `REDIS_GEMINI_QUEUE_RETRY_AFTER`, `REDIS_LORA_QUEUE_RETRY_AFTER`, as duas últimas com a restrição `retry_after > timeout` da regra 16 comentada). Suíte Pest 570/570, `npm run typecheck`/`npm run build` limpos (só warnings pré-existentes de libs Max* no build). **Auditoria de `implementations/` (33 achados, 7 fases) encerrada.**
- **Padronização das abas em MaxTabs declarativo (07/08/2026): Concluída** — `ClientWorkspace.vue`, `SocialMediaEventModal.vue` e `TabThemes.vue` migraram da forma composta de abas do wrapper PrimeVue para a forma declarativa `MaxTabs`/`MaxTabItem` de `@maxvue/max-components-ui`; `MaxTabItem` da lib ganhou as props `disabled` e o slot `#title` para cobrir os casos que a forma composta cobria (abas desabilitadas por status do evento no modal, contador "Artes N/M", badges de contagem nas sub-abas de Temas). Verificação visual (login real via Playwright headless, servido localmente fora do Caddy) confirmou em duas das três telas o CSS customizado aplicado: espaçamento de `.max-tabs-content` correto no workspace (5 abas, badges de status/custo de IA) e sub-abas de Temas com contadores corretos (Pendentes/Agendados/Publicados/Notícias/Arquivadas); no modal do evento a verificação confirmou o comportamento (aba inicial por status, abre em "Artes" para evento `art_ready` com o contador "0/1", troca de aba ativa via `active` attr e hover), mas **não** o CSS customizado — o bloco `.modal-tabs` do modal é `scoped` e as regras `.max-tabs-title`/`.max-tab-item-title`/`.max-tabs-content` foram escritas sem `:deep()`, então ficaram mortas (CSS default da lib renderizava no lugar do visual pretendido). **Correção posterior (revisão whole-branch):** as 3 regras do `.modal-tabs` passaram a usar `:deep()`, alinhado ao padrão já correto do `ClientWorkspace.vue`; a suíte (vue-tsc/vitest) foi reconfirmada verde, mas o visual do modal não foi revalidado via Playwright nesta correção — só o workspace e as sub-abas de Temas têm confirmação visual real. `npx vue-tsc --noEmit`/`npm run build` mantêm os mesmos erros pré-existentes e não relacionados já descritos na Fase 7 acima (`SimulatedSlide`/`EventSlide` e incompatibilidade de tipos `pinia`/`vue-router` entre o `node_modules` do projeto e os pacotes `file:` de `MaxPinia`/`MaxUse`) — confirmados presentes já no commit anterior às 3 tasks de migração; `vite build` isolado (sem o gate do tsc) compila limpo. `npm run test` (Vitest) 13/13. Suíte Pest 562/562 no ponto de partida do branch (backend intocado por esta padronização); `--parallel` não roda neste ambiente por falta de `CREATEDB` do usuário `socialmedia` no Postgres local (limitação de ambiente pré-existente, não desta mudança). **Nota de merge:** este branch bifurcou de antes da Fase 7 e tocou os mesmos 3 arquivos Vue por coincidência de escopo (abas); o merge preservou as duas mudanças (ex.: `EventTabArtwork` mantém tanto a nova estrutura `MaxTabItem` quanto a prop `:client-enabled` da Fase 7).
