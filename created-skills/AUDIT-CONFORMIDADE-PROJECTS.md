# Auditoria de conformidade — created-skills × projects/ (2026-07-27)

94 skills auditadas. Fases 1–5 do runbook `optimize_skills.md`. Problemas listados são os **CONFIRMADOS** pela revisão adversarial (Fase 3): 361 de 383 problemas e 212 de 322 cortes sobreviveram; 132 achados foram REFUTADOS e descartados.

| # | Skill | Estado | Nº problemas | Destino | Descrição dos problemas |
|---|-------|--------|--------------|---------|--------------------------|
| 1 | `laravel-social-media-oauth-token-lifecycle-management-best-practices` | Critica | 8 | CORRIGIR + PODAR (~22%) | Substituir o exemplo por: protected $casts (propriedade, não método) com 'access_token' => 'encrypted' (string simples, não array), mais 'token_expires_at' => 'datetime', 'params' => 'array', 'is_active' => 'boolean', e $hidden = ['access_token'], espelhando app/Models/Calendar/SocialMediaCredent… (+5 cortes de bloat, ~22%) |
| 2 | `laravel-brazilian-payments-integration` | Critica | 7 | CORRIGIR + PODAR (~5%) | Trocar as referências para config('services.asaas.token') por config('services.asaas.api_key') (chave real, já usada em CellChargeService.php). Para o webhook_token, manter a nota de 'a ser criado', mas deixar claro que a convenção de chave real do projeto para este gateway é 'api_key'/'base_url'… (+1 cortes de bloat, ~5%) |
| 3 | `python-concessionarias-automation-best-practices` | Critica | 6 | CORRIGIR + PODAR (~18%) | Substituir o bloco 'Estrutura do payload de callback' (linhas 43-48) pelo contrato real: POST form-encoded para https://engeapp.com.br/python/registra.php com `{'key': projeto['chave_publica'], 'campos': json.dumps(campos)}` (ou `'campo': '<nome_do_campo>'` no formato antigo do sinceti), onde `ca… (+4 cortes de bloat, ~18%) |
| 4 | `typescript-tooling-monorepo-best-practices` | Ruim | 8 | CORRIGIR + PODAR (~40%) | O arquivo tem 335 linhas (não 336). O restante procede: conteúdo 100% type-level, sobreposto ao da skill advanced-types e nunca citado pelo SKILL.md. Passo 4 de validação (SKILL.md:50-56) usa scripts que não existem nos projetos e ignora vue-tsc/tsgo references/tsconfig-strict.json usa baseUrl (l… (+7 cortes de bloat, ~40%) |
| 5 | `vue-vitest-testing-best-practices` | Ruim | 8 | CORRIGIR + PODAR (~14%) | Na seção 4, substituir o bloco vi.mock('@maxvue/max-use') por vi.stubGlobal('apiGetRoute'/'apiPostRoute', vi.fn(...)) — e opcionalmente vi.stubGlobal('route') — explicando que no engeapp esses helpers são globais auto-importados (vite.config.ts + auto-import.d.ts), inexistentes sob o vitest.confi… (+4 cortes de bloat, ~14%) |
| 6 | `laravel-concessionaires-tariffs-regulation-best-practices` | Ruim | 7 | CORRIGIR + PODAR (~18%) | Corrigir a linha 21 do SKILL.md: `ConcessionaireSubsidiaryRegulationsFile`: `Regulations()` (`hasMany` ConcessionaireSubsidiaryRegulation via FK `id_file`); `Concessionaires()` (`belongsTo` ConcessionaireSubsidiary via `id_business`) — não `regulations()`/`business()` em minúsculas. Renomear a sk… (+4 cortes de bloat, ~18%) |
| 7 | `vue-frontend-bug-fixing-best-practices` | Ruim | 7 | CORRIGIR + PODAR (~7%) | Substituir todas as 5 ocorrências de `auto-imports.d.ts`/`components.d.ts` por `auto-import.d.ts` (composables/helpers) e `auto-import-components.d.ts` (componentes), conforme vite.config.ts:104 e :119 do engeapp. Corrigir as linhas 47 e 388: existe sim `vitest.config.ts` na raiz do engeapp cobri… (+2 cortes de bloat, ~7%) |
| 8 | `laravel-pest-testing-best-practices` | Ruim | 6 | CORRIGIR + PODAR (~16%) | Substituir a proibição absoluta por: prefira Unit puro com make()/mocks, mas quando o teste de Unit precisar do banco (ex.: tests/Unit/Models/CallTest.php, tests/Unit/Services/Voip*), isso é aceito no engeapp — o próprio tests/Pest.php aplica DatabaseTransactions também em 'Unit' justamente por c… (+4 cortes de bloat, ~16%) |
| 9 | `vue-rss-news-moderation-dashboard-best-practices` | Ruim | 6 | CORRIGIR + PODAR (~15%) | Além da paginação ausente, o exemplo da store na skill está factualmente errado no shape da resposta: o endpoint retorna `{ items, total }`, então `pending.value = data` (SKILL.md:48-49) deve ser `data.items` + `pendingTotal.value = data.total`. Adicionar: NEWS_PAGE_SIZE=200, params limit/offset,… (+4 cortes de bloat, ~15%) |
| 10 | `laravel-security-hardening-best-practices` | Ruim | 5 | CORRIGIR + PODAR (~7%) | Trocar a instrução da linha 38 para: 'Defina SESSION_SECURE_COOKIE=true, SESSION_HTTP_ONLY=true e SESSION_SAME_SITE=lax (ou strict) no .env de produção — config/session.php já lê esses valores via env() e não deve ser hardcoded.' Incluir menção a same_site como controle anti-CSRF relevante. Corri… (+2 cortes de bloat, ~7%) |
| 11 | `laravel-ziggy-routing-integration-best-practices` | Ruim | 5 | CORRIGIR + PODAR (~5%) | Substituir o exemplo de §5 por uma setup store real: `defineStore('projects', () => { const isCached = ref(true); const data = ref(null); const options = computed(() => ({ get: { route: 'projects.index' }, ... })); return { data, options, isCached } })`, deixando claro que `options` é um computed… (+1 cortes de bloat, ~5%) |
| 12 | `frontend-design-best-practices` | Ruim | 4 | CORRIGIR | Na secao Tipografia (l.71-75), ancorar a regra na realidade do sistema: a fonte base do app e Quicksand, imposta pelo preflight do presetMaxUno (themes/font.scss via all.scss), e as tabelas Max usam Jost; app.blade.php ainda carrega instrument-sans. A orientacao deve ser sobre hierarquia/escala/p… |
| 13 | `php-best-practices` | Regular | 8 | CORRIGIR + PODAR (~35%) | Registrar explicitamente (em SKILL.md e em rules/type-strict-mode.md) que no engeapp o padrão vigente é NÃO usar declare(strict_types=1) — 0 de 840 arquivos em app/ o usam e pint.json não aplica declare_strict_types —, tratando a regra como opcional/aplicável apenas a código novo isolado ou a out… (+7 cortes de bloat, ~35%) |
| 14 | `vue-debugging-best-practices` | Regular | 8 | CORRIGIR + PODAR (~23%) | Cluster SSR/hidratação (seção ### SSR em SKILL.md:180-183 + linhas 94/172/177 e 6 arquivos reference/) é morto para o projeto, que não tem SSR. reference/checkbox-true-false-value-form-submission.md ensina submit de <form> HTML nativo com <input type=checkbox>/<input type=radio>, fluxo inexistent… (+6 cortes de bloat, ~23%) |
| 15 | `laravel-vue-login-maxauthcard-best-practices` | Regular | 8 | CORRIGIR + PODAR (~14%) | Trocar nos dois arquivos (SKILL.md:54 e references/frontend-vue.md:11) os valores por `:forgot-to="{ query: { sub_page: 'forgot-password' } }"` e `:register-to="{ query: { sub_page: 'register' } }"`, acrescentando uma linha explicando que forgotTo/registerTo são RouteLocationRaw do Vue Router (nã… (+3 cortes de bloat, ~14%) |
| 16 | `laravel-best-practices` | Regular | 7 | CORRIGIR + PODAR (~15%) | Remover ou corrigir o parêntese '(sessão em banco)' em rules/security.md:79 — a sessão no engeapp usa o driver 'file' (SESSION_DRIVER=file em .env), não 'database'. O mecanismo de CSRF via cookie XSRF-TOKEN continua correto e independe do driver de sessão; bastaria remover a afirmação específica … (+7 cortes de bloat, ~15%) |
| 17 | `vue-max-stack-frontend-best-practices` | Regular | 7 | CORRIGIR + PODAR (~3%) | Remover o bloco de código axios.defaults (SKILL.md:252-256) e a restrição final "NÃO omita withCredentials/withXSRFToken na configuração do axios do EngeApp" (linha 287). Se algo for mantido, deve dizer que withCredentials é aplicado internamente pelo MaxUse/MaxPinia por requisição e que o app nã… (+1 cortes de bloat, ~3%) |
| 18 | `vue` | Regular | 6 | CORRIGIR + PODAR (~18%) | Atualizar SKILL.md:12 para citar Vue ^3.6.0-rc.2 (versão real em package.json e node_modules do engeapp), não ^3.6.0-beta.17. references/component-slots.md ensina a implementar useMouse manualmente com addEventListener, quando useMouse já existe pronto em @maxvue/max-use, e o arquivo não tem a 'N… (+5 cortes de bloat, ~18%) |
| 19 | `laravel-jobs-queues-horizon-best-practices` | Regular | 6 | CORRIGIR + PODAR (~12%) | Manter a regra como padrao desejado, mas acrescentar ressalva de estado real: 5 dos 12 Jobs com HasAgentAiRequest (AnalyzeProtocolJob, HealthScoreJob, ProcessPromotionAgentJob, Browser/BrowserAiJob, Planner/ExtractProtocolFromCommentJob) e o GeminiContentJob ainda rodam na fila `default`, cujo su… (+4 cortes de bloat, ~12%) |
| 20 | `laravel-ai-agents-ecosystem` | Regular | 6 | CORRIGIR + PODAR (~5%) | Não remover a prescrição, mas calibrá-la à realidade: (1) o que É convenção estabelecida é a chave 'status' ('success'\|'error') + 'message' — 36 das 41 tools usam 'status'; (2) a chave 'data' é OPCIONAL e minoritária (7/41) — tools de leitura frequentemente retornam chaves de domínio no nível rai… (+2 cortes de bloat, ~5%) |
| 21 | `vue-unocss-styling-best-practices` | Regular | 6 | CORRIGIR + PODAR (~5%) | Corrigir a Seção 7: o remapeamento de variáveis em colors.scss compila para `:root .dark` (bloco aninhado em :root, colors.scss:1068-1069), ou seja, a classe `.dark` precisa estar em um DESCENDENTE de <html> (body ou wrapper da app) para o seletor casar. Aplicar `.dark` em document.documentElemen… (+1 cortes de bloat, ~5%) |
| 22 | `vue-max-components-ui-development-best-practices` | Regular | 6 | CORRIGIR | A skill não está factualmente errada ao falar de wrappers PrimeVue 4 — CLAUDE.md afirma explicitamente que 'o código ainda depende do PrimeVue hoje' e a migração ainda não foi executada. O defeito é de OMISSÃO: falta uma subseção na Seção 3 (e menção na description) apontando que existe migração … |
| 23 | `vue-max-ecosystem-api-reference` | Regular | 5 | CORRIGIR + PODAR (~10%) | `cancelLoad(retryInSeconds?)` — Aborta o GET em voo. Se `true`/`0` → reagenda `loadInServer()` após 5 SEGUNDOS; se número > 0 → reagenda após esse número de SEGUNDOS (não ms). Propósito: exibe rótulos de mensagem/erro e o asterisco de obrigatoriedade. (Remover a nota 'Não consta no index.ts' — o … (+5 cortes de bloat, ~10%) |
| 24 | `vue-inputs-masks-validation-best-practices` | Regular | 5 | CORRIGIR + PODAR (~20%) | Substituir o mandato de E.164 estrito (§5 linha 64, §6 linhas 69-78 e a Restrição da linha 101) pelo contrato real do projeto: gravar DDI + dígitos sem '+' (ex.: 5511999999999), que é o que MaxPhoneField emite via v-model e o que PhoneClass::getInternationalPhoneNumber grava. libphonenumber-js (o… (+4 cortes de bloat, ~20%) |
| 25 | `laravel-cache-best-practices` | Regular | 5 | CORRIGIR + PODAR (~9%) | Corrigir as 3 ocorrências: (a) frontmatter linha 3 — trocar "store default database" por "store de cache Redis (phpredis)"; (b) §4 linhas 96 e 99 — o store efetivo é `redis`, que SUPORTA tags; manter apenas a ressalva genérica de que `file`/`database`/`array` não suportam (relevante se alguém tro… (+3 cortes de bloat, ~9%) |
| 26 | `vue-boleto-utils-best-practices` | Regular | 5 | CORRIGIR + PODAR (~16%) | Ajustar linha 41 (e o comentário replicado nas linhas 75-77 do composable) para mencionar que a lib também aceita e normaliza internamente códigos de 36 dígitos (boletos de cartão de crédito Itaú), transformando-os em 47 antes de validar — não apenas 44/46/47/48 recebidos crus. O guard `cleanCode… (+3 cortes de bloat, ~16%) |
| 27 | `vue-pinia-state-management-best-practices` | Regular | 5 | CORRIGIR + PODAR (~15%) | Reescrever a restrição da linha 187 (e alinhar a description do frontmatter) para escopo correto: 'NUNCA use axios.get()/fetch() diretamente; GETs de DADOS DE PÁGINA/ESTADO COMPARTILHADO devem passar por store MaxPinia com isCached = true. Consultas pontuais/one-shot (busca, autocomplete, lookup,… (+3 cortes de bloat, ~15%) |
| 28 | `vue-whatsapp-interactive-messages-simulator-best-practices` | Regular | 5 | CORRIGIR + PODAR (~15%) | Trocar a prescricao de 'siga rigorosamente <style scoped lang="scss">' por 'siga a convencao dominante do projeto: <style lang="scss"> (sem scoped), como em ChatMessageMain.vue'; usar scoped apenas se necessario para evitar colisao de classes. Remover o exemplo de nome curto (icon="open-in-new"/i… (+3 cortes de bloat, ~15%) |
| 29 | `laravel-performance-and-profiling-best-practices` | Regular | 5 | CORRIGIR + PODAR (~9%) | Defina o gate 'viewPulse' (Gate::define) para restringir o acesso a producao; use Pulse::user() apenas para customizar os dados exibidos do usuario (name/email/avatar/extra), nao para autorizacao. Remover a referencia ao comando artisan `log-viewer:clear-cache` (nao existe); a limpeza de cache do… (+2 cortes de bloat, ~9%) |
| 30 | `laravel-media-library-best-practices` | Regular | 5 | CORRIGIR + PODAR (~5%) | Remover ou corrigir a frase 'Isso não está em uso hoje.' na linha 49. O engeapp já usa singleFile() em SupportContact::avatars (disco whatsapp) e Post::cover (disco padrão), portanto a frase deve ser substituída por algo como 'já usado em SupportContact (avatares) e Post (capa) quando se quer um … (+1 cortes de bloat, ~5%) |
| 31 | `laravel-reverb-websockets-best-practices` | Regular | 5 | CORRIGIR + PODAR (~4%) | Remover da linha 12 a referência ao bloco Socket.IO/Adonis e à var VITE_WS_URL; se essa informação for útil, ela pertence a created-skills-adonis/. Manter apenas a parte Laravel (BROADCAST_CONNECTION=reverb, configureReverbEcho, @laravel/echo-vue). A frase deve ser removida: nenhum código do fron… (+1 cortes de bloat, ~4%) |
| 32 | `vue-typescript-best-practices` | Regular | 5 | CORRIGIR + PODAR (~4%) | Remover as três afirmações de que o nome Ziggy 'resolve para a URL /api/...' (SKILL.md linha 67 'que ele resolve internamente para a URL `/api/...`', linha 80 comentário '// nome de rota (Ziggy); resolve para /api/...', linha 108 'resolvem o nome internamente (via route()) para a URL `/api/...`')… (+1 cortes de bloat, ~4%) |
| 33 | `vue-max-use-development-best-practices` | Regular | 5 | CORRIGIR | Reformatar os exemplos de código TypeScript da skill (linhas 14-21 e 32-55) para indentação de 4 espaços, conforme exigido por projects/MaxUse/eslint.config.js:28 (@stylistic/indent) e seguido de fato em src/Composables/useRefCached.test.ts. Ajustar a restrição para proibir apenas `any` não justi… |
| 34 | `laravel-ai-bank-ticket-processing-best-practices` | Regular | 4 | CORRIGIR + PODAR (~18%) | Em §5 (e no item 3 das Restrições), não apresentar `project_id (int)` como contrato válido: registrar que o schema atual usa `integer()` mas a PK de `projects` é ULID char(26) (HasUlids), portanto o campo DEVE ser `$schema->string()` — como já faz SaveTrtBilletToProject.php:80 — sob pena de `Proj… (+5 cortes de bloat, ~18%) |
| 35 | `laravel-scout-searchable-best-practices` | Regular | 4 | CORRIGIR + PODAR (~22%) | Suavizar a verdade-base: HasScoutMeilisearch é o padrão preferencial para novos models, mas existem exceções legítimas no projeto (App\Models\Project\Project e App\Models\SupportChat\SupportProtocol) que usam Laravel\Scout\Searchable cru com toSearchableArray() manual mínimo (poucos campos). Troc… (+4 cortes de bloat, ~22%) |
| 36 | `technical-documentation-best-practices` | Regular | 4 | CORRIGIR + PODAR (~16%) | Sessão de 30 dias é falsa para o engeapp Rate limiting de 100 req/min inventado Config do TypeDoc omite a chave 'plugin' necessária para typedoc-plugin-markdown funcionar (+4 cortes de bloat, ~16%) |
| 37 | `vue-max-components-ui-popovers-confirmations-best-practices` | Regular | 4 | CORRIGIR + PODAR (~15%) | Trocar a proibição absoluta por: o valor de v-tooltip é uma string na maioria dos casos; quando precisar de HTML ou delay, use o objeto do PrimeVue `{ value, escape: false, showDelay, hideDelay, class, autoHide }` (padrão real do engeapp, ex. FormEditModule.vue e ChatStatus.vue). O que NÃO funcio… (+4 cortes de bloat, ~15%) |
| 38 | `laravel-frankenphp-octane-best-practices` | Regular | 4 | CORRIGIR + PODAR (~16%) | Trocar a menção a `octane.warm` por `octane.flush` (bindings que devem ser re-resolvidos a cada requisição) em conjunto com `octane.listeners` (para classes/lógica de reset via eventos), e mencionar explicitamente a chave `flush` do config/octane.php. Remover/corrigir a afirmação de que variáveis… (+3 cortes de bloat, ~16%) |
| 39 | `vue-axios-api-integration-best-practices` | Regular | 4 | CORRIGIR + PODAR (~15%) | Na Regra 3 (SKILL.md linha 20), substituir o snippet simplificado pelo resolver real de resources/app.ts (bloco try que normaliza a URL absoluta do Ziggy para path relativo quando `url.startsWith(window.location.origin)`, com fallback `\|\| '/'`, e catch retornando null), acrescentando uma linha ex… (+3 cortes de bloat, ~15%) |
| 40 | `vue-complex-modal-forms-autosave-best-practices` | Regular | 4 | CORRIGIR + PODAR (~18%) | Em §2, remover `.show()` da lista de métodos prescritos: ele é a action crua `show(id: string)` da useModalStore e, chamado sem o id interno do modal, não abre nada. Prescrever `.toggle()` como forma de abrir/alternar e `is_show` para ler o estado; `.hide()` pode permanecer como fechamento explíc… (+3 cortes de bloat, ~18%) |
| 41 | `laravel-user-impersonation-best-practices` | Regular | 4 | CORRIGIR + PODAR (~6%) | Trocar `->assertExactJson(false)` / `->assertExactJson(true)` por `->assertContent('false')` / `->assertContent('true')` (ou `expect($response->json())->toBeFalse()/->toBeTrue()`) nas três provas Pest das linhas 158, 168 e 183. Corrigir o comentário da linha 92 para "contrato MaxPinia: status.ser… (+2 cortes de bloat, ~6%) |
| 42 | `laravel-vue-geocoordinates-maps-best-practices` | Regular | 4 | CORRIGIR + PODAR (~10%) | Reescrever o bullet 'Validação de DTO' como 'DTOs de tipagem (TypeScript Transformer)': manter a listagem dos campos de CoordinateDecimalData/CoordinateUTMData/CoordinateDMSData/DmsValueData, mas dizer que são Data classes de transferência usadas por LocationCoordinateData (tipagem/geração de tip… (+2 cortes de bloat, ~10%) |
| 43 | `laravel-authorization-policies-gates` | Regular | 4 | CORRIGIR + PODAR (~3%) | No item 5, deixar explícito que $this->authorize() só funciona se o controller usar `use Illuminate\Foundation\Auth\Access\AuthorizesRequests;` (a classe base App\Http\Controllers\Controller é vazia, sem esse trait). Ajustar o snippet do controller para incluir o trait, OU apresentar Gate::author… (+1 cortes de bloat, ~3%) |
| 44 | `laravel-cloud-storage-integrations` | Regular | 4 | CORRIGIR + PODAR (~9%) | A Seção 3 e a restrição sobre path() devem instruir explicitamente: use as helpers globais resolveLocalDiskPath($disk, $relativePath) e saveToRemoteDisk($disk, $relativePath, $content) de app/Helpers/FilesHelpers.php (elas já fazem o branch por driver, cache por requisição e cleanup), em vez de c… (+1 cortes de bloat, ~9%) |
| 45 | `laravel-holiday-sla-calculation-best-practices` | Regular | 4 | CORRIGIR + PODAR (~5%) | A skill (linha 23) afirma que a busca é 'filtrada pelo estado padrão configurado (padrão: 'go' para Goiás)'. Isso é enganoso: como config/api.php já define a chave invertexto_default_state com fallback env(..., false), o segundo argumento 'go' do config() em HolidayService.php:16 é código morto —… (+1 cortes de bloat, ~5%) |
| 46 | `laravel-socialite-oauth-integration-best-practices` | Regular | 4 | CORRIGIR | Na linha 47, trocar o link para o caminho irmão portátil: `[laravel-vue-login-maxauthcard-best-practices](../laravel-vue-login-maxauthcard-best-practices/SKILL.md)`, que resolve tanto em created-skills/backend_laravel/ quanto em <projeto>/.claude/skills/. Exemplo Pest da skill (linhas 100-115) mo… |
| 47 | `vue-auto-import-components-best-practices` | Regular | 3 | CORRIGIR + PODAR (~15%) | Bloco de config da skill omite a entrada { 'ziggy-js': ['route'] } do array imports do AutoImport, e não menciona o alias obrigatório 'ziggy-js' apontando para node_modules/ziggy-js. Exemplo usa <MaxTitle1>Painel de Controle</MaxTitle1> passando o título como slot default, mas o componente não te… (+3 cortes de bloat, ~15%) |
| 48 | `vue-brand-positioning-character-management-best-practices` | Regular | 3 | CORRIGIR + PODAR (~22%) | Reescrever a linha 39 como afirmação factual direta (sem hedge condicional): TabBrandPositioning.vue, TabCharacters.vue e TabThemes.vue usam de fato um bloco <style lang="scss"> não-scoped com classes (ex. .characters-page) e variáveis de tema (var(--background-200)); ao editar esses três arquivo… (+3 cortes de bloat, ~22%) |
| 49 | `vue-router-best-practices` | Regular | 3 | CORRIGIR + PODAR (~34%) | Corrigir SKILL.md:65 e reference/guards-navegacao-next.md:91 para remover 'register' da lista de rotas inexistentes (a rota 'register' existe, registrada automaticamente via glob a partir de RegisterPage.vue) — manter apenas que o destino do redirect guest é sempre { name: 'board' }, e citar como… (+3 cortes de bloat, ~34%) |
| 50 | `laravel-anticaptcha-integration-best-practices` | Regular | 3 | CORRIGIR + PODAR (~8%) | Os logs são roteados para o canal 'anticaptcha' (driver daily), que grava arquivos diários como storage/logs/anticaptcha-YYYY-MM-DD.log (retenção padrão de 15 dias via LOG_DAILY_DAYS). Ao depurar, procure o arquivo do dia correspondente, não um anticaptcha.log fixo. Substituir o bullet de SKILL.m… (+2 cortes de bloat, ~8%) |
| 51 | `laravel-pdf-handling-best-practices` | Regular | 3 | CORRIGIR + PODAR (~10%) | A conversão PDF->imagem tem dois caminhos distintos e não relacionados no engeapp: (1) `App\MediaLibrary\ImageGenerators\Pdf`, registrado em config/media-library.php e usado pelo pipeline do Spatie Media Library; (2) `App\Models\File\File::createThumbnailFromPdf()`, que instancia `Spatie\PdfToIma… (+2 cortes de bloat, ~10%) |
| 52 | `vue-dayjs-date-manipulation-best-practices` | Regular | 3 | CORRIGIR + PODAR (~8%) | Reescrever a Seção 6 para ensinar primeiro os helpers de @maxvue/max-use (auto-importados): addTime(data, 7, 'days'), diffInDays/diffInHours/diffInMinutes, isPast/isFuture/isSameDay/isWeekend/inDateInterval/hasPassedHours e timeAgo — citando a assinatura real (aceitam MaybeRefOrGetter<string\|numb… (+2 cortes de bloat, ~8%) |
| 53 | `laravel-brazilian-localization-best-practices` | Regular | 3 | CORRIGIR + PODAR (~5%) | Reescrever 1.D para documentar o padrão real do backend — `number_format($valor, 2, ',', '.')` com prefixo 'R$ ' quando exibido (ex.: app/Traits/HasAgentAiRequest.php:137, app/Services/Finance/PaymentPricing.php:41-68) — e remover/inverter a Restrição da linha 147 que proíbe concatenar 'R$ '. Se … (+1 cortes de bloat, ~5%) |
| 54 | `laravel-engeapp-project-homologation-best-practices` | Regular | 3 | CORRIGIR + PODAR (~5%) | No exemplo de SKILL.md:47, trocar 'department' => 'homologacao' por um valor real do vocabulário controlado (ex.: 'concessionair_web' para protocolo aberto no site da concessionária, ou 'internal_affairs' para protocolo interno), e citar explicitamente a lista de valores aceitos (internal_affairs… (+1 cortes de bloat, ~5%) |
| 55 | `laravel-global-helpers-best-practices` | Regular | 3 | CORRIGIR + PODAR (~4%) | Corrigir o bloco de código da Seção 5 para refletir a assinatura real: `function ValidateCpf($cpf) : bool` (sem tipo de parâmetro), já que a skill afirma explicitamente ser o "padrão real" e a linha 4 de ValidationHelpers.php diverge tanto no nome quanto na tipagem do parâmetro. Reescrever a Seçã… (+1 cortes de bloat, ~4%) |
| 56 | `vue-toast-notifications-toastify-best-practices` | Regular | 3 | CORRIGIR | Trocar 'ele registra apenas ZiggyVue, pinia, VueFinder, MaxComponentsUi e o router' por 'ele registra ZiggyVue, vaporInteropPlugin, pinia, MaxComponentsUi e o router (VueFinder NÃO é registrado globalmente — o FileManager.vue provê VueFinderOptions localmente)'. Remover a afirmação de que o impor… |
| 57 | `systematic-debugging-best-practices` | Regular | 2 | CORRIGIR + PODAR (~9%) | Substituir o bloco bash placeholder das linhas 83-86 por instrumentação concreta e executável por fronteira do engeapp, reaproveitando os mecanismos que a skill já cita corretamente em outros pontos: (1) camada Vue/MaxPinia — inspecionar `store.data` e `store.status.server.get` (is_requested/is_s… (+3 cortes de bloat, ~9%) |
| 58 | `laravel-finance-coupons-discounts-best-practices` | Regular | 2 | CORRIGIR + PODAR (~8%) | A linha 43 deve reatribuir o cálculo: o desconto por cupom é computado em App\Services\Finance\PaymentPricing::for() (app/Services/Finance/PaymentPricing.php:34-41) com float puro, e a mesma lógica está duplicada em App\Services\Bank\EfiPaymentStatus (linhas 53-60 e 179-186) — risco real de diver… (+2 cortes de bloat, ~8%) |
| 59 | `laravel-livekit-server-sdk-best-practices` | Regular | 2 | CORRIGIR + PODAR (~7%) | No exemplo de LiveKitService::createRoom (SKILL.md:137-147) acrescentar o 3o parametro opcional `?string $metadata = null` com o bloco `if ($metadata !== null) { $options->setMetadata($metadata); }`, e citar na secao 3 que o metadata da sala e o canal usado no projeto para passar contexto ao work… (+2 cortes de bloat, ~7%) |
| 60 | `laravel-trello-api-integration` | Regular | 2 | CORRIGIR + PODAR (~10%) | Reescrever o bullet 'Onde há log e onde não há' (linha 36) e o comentário do exemplo (linhas 120-121) para: (1) o único log no canal `trello` que executa hoje é `Log::channel('trello')->error('Erro ao baixar anexo do Trello', ...)` no catch de `syncMediaForAttachment` (TrelloService.php:185); (2)… (+2 cortes de bloat, ~10%) |
| 61 | `vue-max-use-usecachedapi-state-cache-best-practices` | Regular | 2 | CORRIGIR + PODAR (~10%) | Ajustar a linha 29 para: o nome pontilhado é resolvido por resolveRoute() do @maxvue/max-use, que delega ao resolvedor injetado pela aplicação via setRouteResolver() (em engeapp/resources/app.ts, implementado com route() do Ziggy, que ainda remove o origin da URL absoluta). Correção sobre a acusa… (+2 cortes de bloat, ~10%) |
| 62 | `laravel-typescript-transformer-best-practices` | Regular | 2 | CORRIGIR + PODAR (~15%) | Na Instrução 1, incluir `transformer()` na lista de métodos da API v3 citada, já que é o método usado em TypeScriptTransformerServiceProvider.php:21 para registrar o EnumTransformer — o ponto real de ajuste do pipeline de Enums referenciado na Instrução 3. Reescrever a Instrução 3 (SKILL.md linha… (+1 cortes de bloat, ~15%) |
| 63 | `laravel-api-integration-patterns` | Regular | 2 | CORRIGIR | Separar os dois grupos: `withCache(int $seconds = 0)` / `enableCache(int $seconds = 0)` / `cache(bool $value = true, int $seconds = 0)` habilitam o cache e usam TTL padrao de 120s quando `seconds` <= 0. Ja `cacheTtl(int $seconds = 0)` / `cacheMinutes(int $minutes = 0)` / `cacheHours(int $hours = … |
| 64 | `laravel-meta-graph-api-integration-best-practices` | Regular | 2 | CORRIGIR | Corrigir linha 24 e o blockquote da linha 31 para deixar claro que Socialite EXISTE no projeto (composer.json, SocialiteController) mas é usado apenas para login social do usuário (Google/Facebook) via App\Http\Controllers\Auth\SocialiteController — não para obter ou renovar tokens da Graph API c… |
| 65 | `laravel-power-of-attorney-generation-best-practices` | Regular | 2 | CORRIGIR | O modo do PDF é decidido apenas pelo parâmetro `string $type_document`: o único valor literal testado no código é `'digital'` (linhas 119, 122, 140, 143, 163 de PowerAttorneyService.php); qualquer outro valor cai no ramo físico/manual. Por convenção, o controller chama createPdf com o literal `'b… |
| 66 | `laravel-database-eloquent-best-practices` | Regular | 1 | FUNDIR→laravel-best-practices | Corrigir SKILL.md:21 para refletir o comportamento real: no Laravel 13, a trait SerializesModels já serializa e restaura automaticamente as relações eager-loaded do model ao despachar para um Job (via getQueueableRelations()/loadMissing() em __serialize/__unserialize), então normalmente NÃO é pre… (+3 cortes de bloat, ~18%) |
| 67 | `laravel-redis-integration-best-practices` | Boa | 5 | CORRIGIR + PODAR (~15%) | Corrigir a referência na Seção 3 (linha 81 do SKILL.md) de 'TrtPaymentSchedulingService.php:105' para 'TrtPaymentSchedulingService.php:109' (linha 105 é a assinatura do método schedule(), o Cache::lock está na linha 109). Na Seção 2, sinalizar explicitamente que o código real (geckodriver_ports, … (+4 cortes de bloat, ~15%) |
| 68 | `vue-eslint-stylelint-quality-standards` | Boa | 5 | CORRIGIR + PODAR (~7%) | Remover a menção a 'CI' tanto da description (frontmatter, linha 3: 'ou ao investigar falhas de lint/formatação no CI') quanto da linha 42 ('nem no CI'), pois o projeto engeapp não possui pipeline de CI (sem .github/workflows). Adicionar à lista de regras do Stylelint (seção 'Regras do Stylelint'… (+2 cortes de bloat, ~7%) |
| 69 | `laravel-ai-datasheet-extraction-best-practices` | Boa | 4 | CORRIGIR + PODAR (~27%) | Adicionar uma seção curta de pipeline: File -> GeminiDocumentService::datasheetFile() (LocalDocument application/pdf + promptWithFallback sobre FALLBACK_MODELS, retry só em 503/429/5xx) -> persiste em file.data_ai -> GeminiDatasheetProcessJob converte data_ai em Brand + inverters/modules (updateO… (+2 cortes de bloat, ~27%) |
| 70 | `laravel-electrical-calculations-dimensioning-best-practices` | Boa | 4 | CORRIGIR + PODAR (~3%) | Ajustar o bloco de exemplo do SKILL.md para descrever o retorno real de getWireSize: (a) pode retornar null se nenhuma seção comercial atender ao cálculo; (b) 'current' é a única chave sempre presente; (c) 'drop_voltage', 'efficiency' e 'loss' só existem quando voltage > 0 e current > 0; (d) 'wir… (+1 cortes de bloat, ~3%) |
| 71 | `laravel-gemini-file-api-media-integration-best-practices` | Boa | 4 | CORRIGIR | Adicionar à skill uma orientação explícita de que todo pipeline File API (upload → polling → generateContent → delete) deve rodar dentro de um Job `implements ShouldQueue` processado pelo Horizon, seguindo o padrão de app/Jobs/GeminiContentJob.php (tries/backoff), nunca no ciclo request/response … |
| 72 | `laravel-rate-limiting-best-practices` | Boa | 4 | CORRIGIR | Corrigir SKILL.md:145 para não afirmar que a 6a tentativa "acrescenta a mensagem de lockout" — na prática o corpo da ValidationException é sempre vazio ([]), tanto nas tentativas com credenciais erradas quanto na tentativa bloqueada por rate limit; o único jeito de diferenciar via teste é asserir… |
| 73 | `typescript-billing-core-architecture-best-practices` | Boa | 4 | CORRIGIR | Ajustar SKILL.md:39 para não prometer 'regras de transição' (que não existem no reference), ou então adicionar ao references/contrato-billing-core.md uma tabela explícita mapeando (status atual + evento) -> status resultante para cada uma das 5 transições esperadas, complementando a regra já exis… |
| 74 | `laravel-digital-signatures-integration` | Boa | 3 | CORRIGIR + PODAR (~24%) | O descompasso e apenas no MECANISMO DE FETCH, nao no restante: Storage (SKILL.md:177) e o job com retentativas ja existem de fato (tries=4 + backoff). Corrigir acrescentando uma nota curta em 'Download Resiliente de Documento Assinado' informando que hoje handleSignedDocument (SignatureService.ph… (+4 cortes de bloat, ~24%) |
| 75 | `typescript-best-practices` | Boa | 3 | CORRIGIR + PODAR (~16%) | Remover ou reescrever a linha 8 — não há CLAUDE.md no repo definindo padrões type-first/funcionais/error-handling; a frase deveria ser removida ou substituída por uma afirmação que não aponte para uma fonte inexistente. Remover 'Maxdmin/' do frontmatter description, deixando apenas 'EngeApp Vue 3… (+4 cortes de bloat, ~16%) |
| 76 | `laravel-qrcode-generation-best-practices` | Boa | 3 | CORRIGIR + PODAR (~10%) | Nao e erro de codigo na skill, e uma omissao de gotcha: acrescentar uma frase curta (na secao 2 e/ou 4) avisando que labelText/labelFont/labelAlignment so sao renderizados pelos writers GD (PngWriter, GifWriter, WebPWriter); o SvgWriter aceita o LabelInterface na assinatura mas o ignora, entao o … (+3 cortes de bloat, ~10%) |
| 77 | `typescript-advanced-types-best-practices` | Boa | 3 | CORRIGIR + PODAR (~10%) | Remover a instrução 8 (linha 23). O tema 'tempo de build / compilação incremental' já é escopo declarado da skill typescript-tooling-monorepo-best-practices (SKILL.md:204 desta skill; linhas 81/175-191 da outra). Se algo precisar permanecer, deve ser apenas um ponteiro para aquela skill, não uma … (+3 cortes de bloat, ~10%) |
| 78 | `laravel-task-scheduling-best-practices` | Boa | 3 | CORRIGIR + PODAR (~8%) | Reescrever o bullet da seção 5 para deixar explícito que APENAS o closure de Studio\Totem\Totem::auth() protege a rota (é o único consumido por Totem::check() no middleware Authenticate); o Gate 'viewTotem' definido em AppServiceProvider.php:145 é convenção interna do engeapp (espelha 'viewPulse'… (+2 cortes de bloat, ~8%) |
| 79 | `laravel-vuefinder-media-library-integration` | Boa | 3 | CORRIGIR + PODAR (~7%) | Corrigir a Instrução 1: manter VueFinderBuilder::create(['local' => new LocalFilesystemAdapter($path)], $config) do vendor, mas instanciar a factory LOCAL `new App\Services\VueFinder\ActionFactory($core, $path)` (subclasse de VueFinderActionFactory que exige $basePath e substitui preview/download… (+2 cortes de bloat, ~7%) |
| 80 | `laravel-php-code-quality-tooling` | Boa | 2 | CORRIGIR + PODAR (~15%) | Trocar o exemplo de generics de relacionamento por uma entidade/método fictício neutro (ex.: `Model`/`items()`) ou por um caso real de HasMany do projeto, e explicitar que é uma recomendação aspiracional (o projeto ainda não usa generics em nenhum model), evitando citar `PlannerCard`/`cards()` qu… (+5 cortes de bloat, ~15%) |
| 81 | `laravel-editorial-calendar-event-workflow-best-practices` | Boa | 2 | CORRIGIR + PODAR (~10%) | Reescrever o item 3 para deixar explícito que throwIfIncomplete é usado por CopywriterJob, CopywriterReviewerJob e GraphicEditorJob, mas NÃO por ArtAnalystJob (cujo handle() apenas loga e finaliza com broadcastCalendarUpdate, sem forçar retry via isDone()). No item 3, esclarecer que reportFinalAg… (+2 cortes de bloat, ~10%) |
| 82 | `laravel-gemini-php-sdk-best-practices` | Boa | 2 | CORRIGIR + PODAR (~8%) | Trocar o exemplo da linha 64 de `$data = json_decode($result->text(), true);` para `$data = $result->json();`, que é o método usado de forma consistente e exclusiva em todo o código real do engeapp (Jobs, Handlers e Controllers de Gemini) para extrair a saída JSON estruturada. Trocar a referência… (+2 cortes de bloat, ~8%) |
| 83 | `laravel-browser-automation-webdriver` | Boa | 2 | CORRIGIR + PODAR (~5%) | Em SKILL.md linhas 53-56, 71 e 74: parar de descrever `time_out` como segundos de espera. Descrever como CONTADOR DE TENTATIVAS (time_out*2 tentativas consecutivas) e documentar explicitamente a armadilha: `sleep(0.5)` no loop (Browser.php:136) recebe int, logo dorme 0s e emite deprecation em PHP… (+1 cortes de bloat, ~5%) |
| 84 | `vue-keyboard-shortcuts-navigation-best-practices` | Boa | 1 | CORRIGIR + PODAR (~18%) | A Instrução 5 (linhas 61-63) é orientação WAI-ARIA genérica (tabindex, setas de navegação) sem nenhuma ocorrência correspondente no código real do projeto (engeapp/MaxComponentsUi). Deveria ser removida ou substituída por uma diretriz concreta ligada a componentes Max* existentes, já que hoje é u… (+3 cortes de bloat, ~18%) |
| 85 | `laravel-migrations-seeders-factories-best-practices` | Boa | 1 | CORRIGIR | Adicionar 'use App\Models\Project\Project;' ao bloco de imports do exemplo em SKILL.md (linhas 73-77), logo após 'use App\Models\Finance\Payments;', para que o exemplo copiável não gere Class not found ao referenciar Project::factory(). |
| 86 | `laravel-whatsapp-cloud-api-integration` | Boa | 1 | CORRIGIR | Instancie com config, nunca com tokens hardcoded: o construtor aceita um array associativo com defaults para seis chaves (from_phone_number_id, access_token, business_id, graph_version, client_handler, timeout), mas o projeto instancia usando apenas duas (from_phone_number_id e access_token). Lei… |
| 87 | `typescript-max-banks-efi-gateway-best-practices` | Boa | 1 | CORRIGIR | Trocar 'a Efí é integrada exclusivamente pelo SDK PHP efipay/sdk-php-apis-efi (Laravel)' por algo como 'a Efí é integrada majoritariamente pelo SDK PHP efipay/sdk-php-apis-efi (Laravel), mas o engeapp também possui um cliente HTTP próprio (app/Http/Integrations/Efi/) para parte dos endpoints — em… |
| 88 | `laravel-brazilian-data-queries-best-practices` | Excelente | 0 | PODAR (~8%) | Conformidade OK; 2 corte(s) de bloat confirmados (~8%). |
| 89 | `laravel-service-providers-dependency-injection-best-practices` | Excelente | 0 | PODAR (~8%) | Conformidade OK; 2 corte(s) de bloat confirmados (~8%). |
| 90 | `laravel-multitenancy-data-isolation-best-practices` | Excelente | 0 | PODAR (~3%) | Conformidade OK; 1 corte(s) de bloat confirmados (~3%). |
| 91 | `vue-meta-api-oauth-integration-best-practices` | Excelente | 0 | PODAR (~5%) | Conformidade OK; 1 corte(s) de bloat confirmados (~5%). |
| 92 | `laravel-exception-handling-logging` | Excelente | 0 | MANTER | Nenhuma correção necessária. |
| 93 | `laravel-services-best-practices` | Excelente | 0 | MANTER | Nenhuma correção necessária. |
| 94 | `laravel-telescope-debugging-best-practices` | Excelente | 0 | MANTER | Nenhuma correção necessária. |